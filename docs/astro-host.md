# Astro Host

The Astro demo shell lives in `web-astro/`.

**The stage bridge is no longer Astro-specific.** It lives in the shared stage package:

→ **[web-stage.md](./web-stage.md)** (`@zt-ui/stage` in `web-stage/`)

Astro is one chrome host among several (Next.js, vanilla). Use that doc for mount API, contracts, and multi-host wiring.
